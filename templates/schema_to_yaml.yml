defaults: &defaults
# => paperclip/attachment_fu (attachment_fu not yet implemented)
  attachment_plugin:  'paperclip'
# => default/resource_controller
  controller_pattern: 'resource_controller'
    
development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults